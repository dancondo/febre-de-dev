%h1 Posts#show
%p Find me in app/views/posts/show.html.erb
= @post.title
= @post.content
- if user_signed_in? && current_user.admin
    = link_to 'Deletar', admin_post_path(@post), class: 'btn btn-danger', method: :delete